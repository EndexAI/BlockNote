---
title: BlockNote with Ariakit
description: Ariakit rich text editor with BlockNote
imageTitle: BlockNote with Ariakit
---

import { Example } from "@/components/example";
import { Callout } from "nextra/components";

## Using Ariakit with BlockNote

[Ariakit](https://ariakit.org/) is an open-source library of unstyled (headless), primitive components with a focus on Accessibility. To use BlockNote with Ariakit, you can import `BlockNoteView` from `@endexai/blocknote-ariakit` (instead of from `@endexai/blocknote-mantine`).

You can fully style the components with your own CSS, or import the provided default styles using the CSS file from `@endexai/blocknote-ariakit/style.css`.

<Example name="basic/ariakit" />
